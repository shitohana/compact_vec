[package]
name = "compact_vec"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", optional = true }

[dev-dependencies]
rmp-serde = "1.3.1"

[features]
serde = ["dep:serde"]
